{
  "name": "openairtwin-browser-regression-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ci": "playwright test --grep-invert snapshots",
    "test:tutorial-site": "playwright test --config tutorial-site.config.js",
    "test:update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@playwright/test": "1.61.1"
  }
}
